import { PreventOutsideClickDirective } from './prevent-outside-click.directive';

describe('PreventOutsideClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventOutsideClickDirective();
    expect(directive).toBeTruthy();
  });
});
