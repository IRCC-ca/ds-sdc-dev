# version: 0.1

# phases:
#   install:
#     commands:
#       - echo installing nodejs...
#       - curl -sL https://deb.nodesource.com/setup_12.x | bash -
#       - apt-get install -y nodejs
#       # - echo installing yarn...
#       # - curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | apt-key add -
#       # - echo "deb https://dl.yarnpkg.com/debian/ stable main" | tee /etc/apt/sources.list.d/yarn.list
#   pre_build:
#     commands:
#       - echo installing dependencies...
#       - npm i -g @angular/cli
#       - npm install
#   build:
#     commands:
#       # - echo testing...
#       # - echo building...
#       - ng build
# artifacts:
#   files:
#     - "**/*"
#   discard-paths: no
#   base-directory: dist/component-library

version: 0.1

phases:
  install:
    commands:
      - apt-get update
      - echo '-k' > ~/.curlrc
  pre_build:
    commands:
      - echo installing dependencies...
      - n 16.16.0
      # - npm i -g @angular/cli@14.1.3
      - npm i --prefix ./projects/ircc-ds-angular-component-library
      # - npm run cypress:run:quick
  build:
    commands:
      - ng build ircc-ds-angular-component-library
      - npm i
      - ng build
artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: dist/ircc-ds-angular-component-library

cache:
  paths:
    - "/root/.npm/**/*"
    - "node_modules/**/*"
